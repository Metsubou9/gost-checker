# `scripts/` — служебные скрипты

- `make-fixtures.mjs` (`npm run fixtures`) — генерирует `public/samples/gost-good.docx` и `gost-bad.docx` для тестов и демо.
- `make-screenshots.mjs` — делает скриншоты интерфейса в `docs/screenshots/` для README.
