# `public/samples/` — примеры документов

- `gost-good.docx` — документ без нарушений (для проверки «всё зелёное»).
- `gost-bad.docx` — документ с типичными нарушениями (для демо отчёта).

Перегенерировать: `npm run fixtures`.
